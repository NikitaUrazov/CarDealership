<UserControl x:Class="CarDealership.Views.BrandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarDealership.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="5">
                <Button Style="{StaticResource PagingButtonStyle}" Content="Назад" Command="{Binding PrevPageCmd}" Margin="5"/>
                <TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center" Margin="5"/>
                <TextBlock Text="/" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding TotalPages}" VerticalAlignment="Center" Margin="5"/>
                <Button Style="{StaticResource PagingButtonStyle}" Content="Вперёд" Command="{Binding NextPageCmd}" Margin="5"/>
            </StackPanel>

            <DataGrid ItemsSource="{Binding Brands}" 
                      AutoGenerateColumns="False"
                      IsReadOnly="True" 
                      DockPanel.Dock="Top"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Top">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="auto"/>
                    <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>
