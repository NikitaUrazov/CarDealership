<UserControl x:Class="CarDealership.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarDealership.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" Margin="10">

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <ComboBox ItemsSource="{Binding AvailableYears}"
                SelectedItem="{Binding SelectedYear}"
                Width="120"
                Margin="0 0 0 10"/>
            <Button 
                Margin="5 0 0 10"
                Content="Экспорт"
                Command="{Binding ExportCmd}"/>
        </StackPanel>
        
        <DataGrid ItemsSource="{Binding Sales}" AutoGenerateColumns="False" Height="400">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Модель" Binding="{Binding ModelName}" Width="200"/>
                <DataGridTextColumn Header="Январь" Binding="{Binding January}" Width="100"/>
                <DataGridTextColumn Header="Февраль" Binding="{Binding February}" Width="100"/>
                <DataGridTextColumn Header="Март" Binding="{Binding March}" Width="100"/>
                <DataGridTextColumn Header="Апрель" Binding="{Binding April}" Width="100"/>
                <DataGridTextColumn Header="Май" Binding="{Binding May}" Width="100"/>
                <DataGridTextColumn Header="Июнь" Binding="{Binding June}" Width="100"/>
                <DataGridTextColumn Header="Июль" Binding="{Binding July}" Width="100"/>
                <DataGridTextColumn Header="Август" Binding="{Binding August}" Width="100"/>
                <DataGridTextColumn Header="Сентябрь" Binding="{Binding September}" Width="100"/>
                <DataGridTextColumn Header="Октябрь" Binding="{Binding October}" Width="100"/>
                <DataGridTextColumn Header="Ноябрь" Binding="{Binding November}" Width="100"/>
                <DataGridTextColumn Header="Декабрь" Binding="{Binding December}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Предыдущая" Command="{Binding PrevPageCmd}" Margin="5"/>
            <TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center" Margin="5"/>
            <TextBlock Text="из" VerticalAlignment="Center" Margin="5"/>
            <TextBlock Text="{Binding TotalPages}" VerticalAlignment="Center" Margin="5"/>
            <Button Content="Следующая" Command="{Binding NextPageCmd}" Margin="5"/>
        </StackPanel>
    </StackPanel>
</UserControl>
